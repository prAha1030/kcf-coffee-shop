spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;NON_KEYWORDS=VALUE
    driver-class-name: org.h2.Driver
    username: test
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6380
      timeout: 2000ms

kafka:
  bootstrap-servers: localhost:9092,localhost:9093,localhost:9094

jwt:
  secret: jwt-your-secret-kcf-coffee-shop-spring-local-key-consds-test